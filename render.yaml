services:
  - type: web
    name: chesticals-server
    env: node
    buildCommand: npm install express socket.io
    startCommand: node server.js
    plan: free
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
